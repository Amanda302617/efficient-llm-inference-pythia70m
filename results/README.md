# Results

This directory stores experiment outputs generated by the evaluation scripts.

Expected generated files:

- `baseline_ppl.json`
- `baseline_speed.json`
- `summary.json`
- `summary.csv`
- `summary_table.md`

JSON, CSV, and PNG result files are ignored by git so that only real local run
outputs are created when experiments are executed.
